import styled from 'styled-components';

export const FigureElement = styled.figure`
  position: relative;
  clear: both;
  outline: 0;
  box-sizing: border-box;
  user-select: auto;
  margin: 0;
  display: block;
`;
