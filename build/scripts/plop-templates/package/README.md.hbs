# GrowCss {{kebabCase name}}

## Installation

```bash
npm install @growcss/{{name}}
```

or if you're using yarn

```bash
yarn add @growcss/{{name}}
```

## Usage

To implement {{kebabCase name}} component into your project you'll need to add the import:

```js
import { {{kebabCase name}} } from '@growcss/{{name}}';
```

After adding import into your project you can use it simply like:

```jsx
<{{kebabCase name}} />
```
