dist: trusty
language: node_js

node_js:
  - 10.14.1

env:
  - SKIP_CHECK=true

cache:
  yarn: true
  directories:
    - node_modules

before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash
  - export PATH="$HOME/.yarn/bin:$PATH"
  - node --version && yarn --version

install:
  - yarn --frozen-lockfile

before_script:
  - yarn run bootstrap
  - yarn run build

script:
  - yarn run test --runInBand

jobs:
  include:
    - stage: Test # test for v10.14.1
    - stage: Test
      node_js:
        - node
    - stage: Test
      node_js:
        - lts/*

    - stage: Coding standard
      script:
        - yarn lint

    - stage: Coverage
      script:
        - yarn global add codecov
        - yarn test:coverage
        - codecov

notifications:
  email:
    on_success: never
    on_failure: change
